import React from "react";

const Layout = () => {
    return (
        <div className="page">
            <h1>
                LAYOUT
            </h1>
        </div>
    );
};

export default Layout;
